{
  "name": "chapter10-client-fetch-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rm -rf dist && vite",
    "build:client": "vite build --outDir dist/client",
    "build:server": "vite build --outDir dist/server --ssr App.tsx",
    "build": "rm -rf dist && npm run build:client && npm run build:server",
    "start": "tsx watch server/index.ts"
  }
}
